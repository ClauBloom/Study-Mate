---
name: lesson-design
description: 课件设计规范（课件的唯一约束来源）：必须做到的要素、引导式实操（载体由总控与学生确定）、讲法倾向、记忆与难度、贯穿线索、复用与骨架。讲解角色与总控加载。
---

# 课件设计规范

课件的**唯一约束来源**。分两类：**要素** 与**倾向**（弱约束，可被覆盖）。

# 一、要素

1. **真实场景开场**：从学生熟悉的真实问题切入，不先抛定义、不先讲底层细节。
2. **术语先来历、后定义**：第一次出现前，先讲它解决什么问题、为什么需要、历史来历，再给定义；中英对照。
3. **权威教材的深度**：讲透前提、推导、边界与反例，不做速成课；内容长没关系，简洁不等于浅。
4. **引导式实操**：每个知识点配一次动手实操，"我带你一步步做"，不把成品甩给学生；载体由总控与学生确定，内核是第三节五条。

# 二、讲法倾向（弱约束，可被覆盖）

问题感 — 先让学生感到"这是个什么麻烦"；具体先行 — 例子在定义前面；小步 — 一次给一点；学生的语言 — 沿用他自己的说法；参与 — 留出预测、改一行、复述的空间；连回项目 — 把知识与他在做的事接上。

**覆盖关系**：以上都是默认值，学生的明确要求、科目性质、任务紧急度可覆盖；`MEMORY.md` 的个人偏好优先级最高。学生反馈只经总控转达（改课件见 learning-coach），本规范不写现场应对。

# 三、实操规范（引导式）

**载体由总控与学生确定**（盘问时确认，记进 `lab/README.md`）；本节只管载体定下来后怎么做。

**通用原则（五条）**：分步引导（一小步讲解+立刻动手，不一次抛完整任务）；亲手做（任务留白给学生写，成品只放 `lab/solutions/`）；可验证（断言/测试/期望输出对照/清单勾选）；即时反馈（做完立刻能验，不等 agent）；卡壳先引导（读错误信息→提示→线索，不给答案）。

**每种载体都要做到**：教程部分带结果（Python 预执行输出，其他语言先跑一遍或贴运行结果）；任务标 **▸ 你的任务**（要做什么、怎么算过）；自检可跑（能自动验的写断言/测试，不能的给自查清单）；答案折叠（做完再展开对照，不提前剧透）。

**结构**：`lab/NN-主题/`（或 `NN-主题.ipynb`）与课件编号对齐；答案统一放 `lab/solutions/`；环境与依赖写 `lab/README.md`。

# 四、记忆与难度（弱约束）

分清**流畅度**（当场会复述）与**存储强度**（过段时间还记得住，目标）；用**合意困难**设计：检索练习（回忆而非重读）、间隔（分散到多次会话）、交错（技能练习混合相关主题）；难度落点始终在最近发展区。

# 五、贯穿线索

每课连回三条：**最终目标**（这段知识与 `MISSION.md` 什么关系）；**配套项目**（从早期就作为实操素材逐课埋线，项目由总控盘问时定）；**工具对照**（编程类：亲手实现与现成库结果对照）。

# 六、复用与骨架（硬约束）

先读 `<subject_path>/assets/` 组件库；从 `<root>/templates/lesson.html` 拷起（顶栏、主题开关、提问提示、脚本引用已就位），**别删主题开关**；编号递增 `NNNN-dash-case.html`；新组件写进 `assets/` 不内联；自包含 HTML（相对路径引用共享层与科目组件），浏览器直开可用；排版干净、适合反复回看。
