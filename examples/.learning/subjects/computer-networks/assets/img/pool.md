# 计算机网络 · 图片库索引

> 课件用图前先按「主题标签」检索本表。位图放 `assets/img/pool/`，这里是它的唯一入口。
> 本课的示意图（分层与封装、以太网帧字段、子网划分、逐跳转发）都是课件正文里的 `::: svg` 内联图，
> 不占位图文件，所以下表目前为空；等需要真实照片或界面截图时再按采集规则补行。

| 文件 | 主题标签 | 一句话说明 | 来源 URL | 许可 | 尺寸 | 抓取日期 |
| --- | --- | --- | --- | --- | --- | --- |

## Gaps

- 缺「真实交换机 / 网线面板 / 路由器背面」这类实物照片。第 2 课讲端口与转发表时用内联示意图替代，
  没有实拍图不影响理解；日后要补，按命名规则 `<主题>-<子主题>-<要点>-<来源缩写>-<NN>.<ext>` 追加一行。
- 缺抓包界面的截图（Wireshark 的三段式展开）。第 5 课实验会实际打开工具，用截图的意义不大，
  暂不采集。
