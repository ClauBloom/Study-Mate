# 计算机网络 · 图片库索引

图片库目录：`assets/img/pool/`（本索引是它的兄弟文件，不是 `pool/pool.md`）。
命名规则：`<主题>-<子主题>-<要点>-<来源缩写>-<NN>.<ext>`，主题词取自 `GLOSSARY.md`。

| 文件 | 主题标签 | 一句话说明 | 来源 URL | 许可 | 尺寸 | 抓取日期 |
| --- | --- | --- | --- | --- | --- | --- |

## Gaps

- 本课三节的配图全部用内联 SVG 示意图（分层与封装、以太网帧字段、MAC 地址位结构、子网划分、逐跳转发），
  这几张图要表达的是结构与字段关系，采集来的位图（机房照片、设备面板）帮不上忙，因此图片库暂时为空。
- 还没找到可用的本机抓包截图素材：Wireshark 界面截图涉及抓包文件与真实地址，等实验课（`net.experiment`）
  自己抓一份再决定要不要入库。
