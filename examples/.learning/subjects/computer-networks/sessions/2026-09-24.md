---
date: "2026-09-24"
subject: 计算机网络
session_goal: 学完链路层与以太网帧，开始 IP 地址、子网与路由
learned:
  - 能逐字段读出一个以太网帧，认出目的 MAC、源 MAC 与类型字段，并说出前导码不计入帧长
  - 用 Python 从十六进制串里切出帧字段，自己算出了那 6 个填充字节的来历
  - 开始子网计算：写完了 ip_to_int，卡在掩码的位运算上
next_step: 写完 lab 的 network_of 与 usable_range，把 T3、T4 两组测试跑绿
weaknesses:
  - 掩码不一定是 8 的倍数，遇到 /26 会退回「按点切字符串」的习惯
  - 交换机为什么不看 IP 还说不完整，等第 3 课讲完逐跳转发再回头验
memory_updates:
  - 看不见的过程用「抓一段真实字节 + 自己切片算」比画框图有效，后面 TCP 也照这个路子讲
---

# 2026-09-24 会话摘要

## 本次要点

- 第 2 课（链路层与以太网帧）：读完正文并做完页内题；用 64 字节的真实帧做了练习，四个问题都答出来了，
  其中「帧载荷 46 字节、IP 总长度 40 字节，差的 6 个字节是填充」是他自己从上一节的 46 字节推出来的
- 第 3 课（IP 地址、子网与路由）：引入用「配错网关的机器」开场，读到前缀长度与网络地址的位运算；
  lab 已下发，五个任务里 T1 写完，T2 只差非法输入的校验
- 局部提问一次：`/26` 的掩码为什么是 `255.255.255.192`、网络地址为什么落在 `192.168.1.64`。
  按具体数字走了一遍按位与之后接受，已记进误解库（`topic: 子网掩码的算法`，importance high）

## 卡在哪

- 位运算的直觉还不够：知道「掩码是与运算」，一动笔又想按 8 位一组切。lab 的 T3 就是为这个准备的，
  写完 `network_of` 再用标准库对一遍
- 交换机与路由器的分工：能背出「交换机看 MAC、路由器看 IP」，但说不清同一个交换机下的两台机器
  为什么不过路由器。这一条留到第 3 课讲完逐跳转发再回头验，别急着收

## 下次从哪继续

先跑 `cd lab/0003-ip-subnet && python3 -m unittest -v` 看 T3、T4 红了哪几条，做完再回到课件的
「把开头的机器修好」那一节，对照答案收尾。
