---
date: "2026-09-24"
subject: 线性代数
session_goal: "学完第 2 课（矩阵与线性变换），并开始第 3 课（高斯消元与解的结构）的 lab"
learned:
  - 矩阵的每一列是基向量变换后的落点，整张数表由两个基向量的去向决定
  - 秩是变换后空间的维数，也是主元的个数、线性无关的列数——三种说法是同一件事
  - 零空间是被变换送到零向量的输入向量集合，秩 + 零空间维数 = 列数
  - 消元的三类行变换（交换、倍乘、相加）不改变方程组的解集；增广矩阵把等号右边同步带上
  - 行阶梯形里出现 [0 … 0 | 非零] 就是无解；没有矛盾行时，主元少于未知量个数就有自由变量
next_step: "第 3 课 lab：把 pivot_columns / rank / solve 三个函数实现完，让 22 条断言全绿；然后进第 4 课（基、维数与坐标）"
weaknesses:
  - 零空间的维数用错过行数，把秩-零化度定理记成了「行数 − 秩」，需要再复述一次并用例子验证
  - 手算消元在「第 3 行 ÷ 常数」那一步忘了把右端项一起算，属于同步性疏忽，不是概念问题
  - 3×3 矩阵的秩与零空间还没有独立算过一遍
memory_updates:
  - "教学偏好补一条：改错比讲对更有效——他现在会自己造反例来验证刚学的规则（今天用两列成比例造了秩 1 的例子）"
  - "抽象符号的卡点具体化了：公式里的「维数」要指明是行数还是列数，否则他会默认成矩阵的行数"
---

## 这次会话做了什么

一次 40 分钟的会话，前半段学完第 2 课,后半段开了第 3 课的 lab。

1. **第 2 课**（`matrix.transform`）：从图像斜切进场，把矩阵读成动作。他的接受路径是先画图、
   再说话，所以这一课的示意图（基向量 `e₁` 不动、`e₂` 向右倒）用上了。秩的几何解释一次就通；
   零空间维数先答错（用了行数），让他直接解 `x + 2y = 0` 之后自己纠正。
2. **第 3 课**（`system.elimination`）：课件过了一遍，讲到「消元后只看两件事：有没有矛盾行、
   有几个主元」。他跟我一起手算了一个 3×3 的例子，在「第 3 行 ÷ 5」那一步忘了同步右端项，
   自己发现后重算了一遍——这个错正好是课件里 `warn` 卡讲的那个。
3. **lab 开了一个头**：`lab/0003-system-elimination/gauss.py` 里 `rref` 已经写出来并跑了测试
   （`TestRref` 6 条通过），`pivot_columns`、`rank`、`solve` 还是 `NotImplementedError`。

## 评估记录的变化

- 第 1 课 `vector.space` 的阶段评估在 9 月 22 日完成，结论「通过」、掌握度 **0.8**，
  已写进 `assessments/0001-vector.space.md`。这次没有新的评估。
- 第 2 课这次只学不评（按「攒 3~5 个节点做一次阶段评估」的习惯，`space.basis` 学完之后
  把 `matrix.transform` 与 `system.elimination` 一起评）。

## 下次从哪继续

先把 lab 做完——那是第 3 课的交付物，也是第 4、5 课的工具。做完之后回课件答那三道题，
再进 `space.basis`。零空间那个误解我会在 lab 做完后让他口头复述一次「自由变量个数 = 列数 − 秩」。
