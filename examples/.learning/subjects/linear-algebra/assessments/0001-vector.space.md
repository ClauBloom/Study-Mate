---
node: vector.space
date: "2026-09-22"
layer: L3
questions:
  - q: "给出方程组 2x + y = 4、x − y = −1，说出它的两个列向量与右端项，并解释这个方程组为什么有唯一解。"
    kind: 概念
    answer: "列是 (2, 1) 与 (1, −1)，右端项是 (4, −1)。两个列向量不共线（一个是 (2,1) 方向，一个是 (1,−1) 方向），所以它们张成整个平面，右端项一定能拼出来；独立方向有两个、未知量也正好两个，拼法只有一种，所以唯一解。(1, 2) 代进去验证成立。"
    verdict: 通过
    note: "对上了判分要点里的「把方程组读成线性组合」与「不共线 → 张成整个平面」两条：他先在纸上画出两个箭头，确认不共线，再动手解。验证那一步是自己补的（我没要求），说明已经把它当作必要步骤。"
  - q: "如果三个三维向量落在同一个过原点的平面上，它们线性相关还是无关？说清理由。"
    kind: 概念
    answer: "线性相关。因为平面只有两个独立方向，第三个向量必定能由前两个拼出来（或者三者互相拼得出来），于是有一组不全为零的系数使组合为零向量，按定义就是线性相关。"
    verdict: 通过
    note: "答案与关键理由都对。他先画了一个平面加三个箭头，再补了「两个独立方向」这句话——用的是几何图像而不是背定义，正是本节点想验的东西。"
  - q: "【排错】你有两个列向量 (1, 2) 与 (2, 4)，右端项取 (3, 7)。下面的说法哪里不对：「两列成比例，所以这个方程组有无穷多解」？"
    kind: 实操
    answer: "错在后半句。两列成比例只说明它们线性相关、拼法可能不唯一，但没有保证右端项能被拼出来。(1, 2) 与 (2, 4) 的全部组合都落在 y = 2x 这条直线上，而 (3, 7) 不在这条线上，所以这个方程组其实无解。把右端项换成 (3, 6) 之后才有无穷多解。"
    verdict: 部分通过
    note: "缺的那一条：一开始把「相关」直接等同于「有无穷多解」，没有先检查右端项在不在张成里。提示「(3, 7) 能不能写成 (1, 2) 的倍数」之后，他立刻自己算出无解，并把「换成 (3, 6) 就有无穷多解」补了出来。结论与更正都对，但需要一次提示，所以不判完全通过。"
mastery: 0.8
verdict: 通过
next: "进入 matrix.transform：把矩阵读成对空间的变换，并用秩与零空间把「解集有多大」讲清楚。第 3 课 lab 里数主元的 pivot_columns 就是把这里的「独立方向有几个」写成代码。"
misconceptions:
  - 线性相关与解的存在性
---

## 这次评估做了什么

阶段评估，1 次会话 3 道题（其中第 3 题兼作排错）。题目对着节点 `vector.space` 的
objective 出：能把方程组读成列向量的线性组合，并说清线性无关的几何含义。
第 3 题的素材来自第 1 课练习里他自己卡住的那个方程组，所以是真实卡点，不是编出来的陷阱。

## 逐题记录

**第 1 题（概念）**：作答原文见 front matter 的 `answer`。他先画图、再说话，顺序是对的。
通过。

**第 2 题（概念）**：几何语言清楚，「平面只有两个独立方向」这句话是这次评估里最有力的证据：
他真的在用维度数数，而不是背「三个向量在三维里才可能无关」这类句子。通过。

**第 3 题（实操 / 排错）**：见 front matter 的 `note`。第一遍把「相关」与「有无穷多解」等同了
（这次暴露的误解 topic 是「线性相关与解的存在性」）；给了一次提示之后自己修正，并补出了正确
情形。判部分通过，但这个误解让本节点的掌握度定在 0.8 而不是更高。

## 结论与下一步

三题里两题一次通过、一题提示后通过，掌握度 **0.8**，节点状态置为**能独立应用**。
下一步进 `matrix.transform`；排错暴露的那个误解已经写进 `progress.yaml` 的 `misconceptions`
与 `misconceptions.yaml`，第 2 课讲零空间时会再碰一次——到时候让他自己说「相关」能推出什么、
推不出什么。
