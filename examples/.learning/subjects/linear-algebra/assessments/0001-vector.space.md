---
node: vector.space
date: "2026-09-22"
layer: L3
questions:
  - q: "给出方程组 x + 2y = 4、2x + y = 5，说出它的两个列向量与右端项，并解释这个方程组为什么有唯一解。"
    kind: 概念
    answer: "两列是 (1, 2) 与 (2, 1)，右端项是 (4, 5)。两个列向量不成比例，所以线性无关，它们张成整个平面；右端项一定拼得出来，而且拼法只有一种，所以是唯一解。解出来 x = 2、y = 1，代回去两个方程都成立。"
    verdict: 通过
    note: "对上判分要点里的两条：把方程组读成列向量的组合、用「独立方向有两个」解释唯一性。他说完列视角之后自己补了一句「两个方向正好对上两个未知量，所以拼法被定死」，这是我想听到的机制，不是背结论。"
  - q: "三支向量落在同一个过原点的平面上，它们线性相关还是无关？说清理由。"
    kind: 概念
    answer: "线性相关。平面只有两个独立方向，第三支向量的方向一定能由前两支拼出来；把它移到等号另一边就得到一组不全为零的系数，使线性组合等于零向量，按定义就是相关。比如 (1, 0)、(0, 1)、(1, 1)，第三支等于前两支之和。"
    verdict: 通过
    note: "答案、理由、例子三条齐全。他用的是「独立方向只有两个」这个说法，而不是「三维空间里三个向量才可能无关」这类句子，说明几何图像已经建立起来。"
  - q: "【排错】两列是 (1, 2) 与 (2, 4)，右端项取 (3, 7)。下面这句话哪里不对：「两列成比例，所以这个方程组有无穷多解」？"
    kind: 实操
    answer: "错在后半句。两列成比例只说明它们线性相关、拼法可能不唯一，没有保证右端项拼得出来。(1, 2) 与 (2, 4) 的全部组合都落在直线 y = 2x 上，而 (3, 7) 不在这条直线上，所以这个方程组无解。把右端项换成 (3, 6) 之后才有无穷多解。"
    verdict: 部分通过
    note: "第一遍把「相关」直接当成「有无穷多解」，漏了「右端项在不在张成里」这一步。提示他「(3, 7) 是不是 (1, 2) 的倍数」之后，他立刻算出无解，并自己补出了换成 (3, 6) 的情形。结论与更正都对，但需要一次提示，所以不判完全通过；这也让本节点的掌握度停在 0.8。"
mastery: 0.8
verdict: 通过
next: "进入 matrix.transform：把矩阵读成对空间的变换，用秩与零空间把「解集有多大」讲清楚。第 3 课 lab 里的 pivot_columns 就是把这里的「独立方向有几个」写成代码。"
misconceptions:
  - 线性相关与解的存在性
---

## 这次评估做了什么

阶段评估，一次会话 3 道题（第 3 题兼作排错）。题目对着节点 `vector.space` 的 objective 出：能把
方程组读成列向量的线性组合，并说清线性无关的几何含义。第 3 题的素材来自第 1 课练习里他自己做过的
那个方程组，所以是真实卡点，不是临时编的陷阱。

## 逐题记录

**第 1 题（概念）**：作答原文见 front matter 的 `answer`。他先写列、再说「两个独立方向对上两个
未知量」，顺序与课件里教的一致；验证那一步是自己补的。通过。

**第 2 题（概念）**：几何语言清楚，「平面只有两个独立方向」是全次评估里最有力的一句证据——他在用
维度数数，而不是背「三支向量在三维空间才可能无关」这种句子。通过。

**第 3 题（实操 / 排错）**：见 front matter 的 `note`。第一遍把「相关」等同于「有无穷多解」，暴露
的误解 topic 是「线性相关与解的存在性」；一次提示后自己修正并补出正确情形。判部分通过。

## 结论与下一步

三题里两题一次通过、一题提示后通过，掌握度定 **0.8**，节点状态置为**能独立应用**。没有把掌握度
定得更高，原因就是第 3 题那一次提示。下一步进 `matrix.transform`；那个误解已写进 `progress.yaml`
的 `misconceptions` 与 `misconceptions.yaml`，第 2 课讲零空间时会再碰一次——到时候让他自己说
「相关」能推出什么、推不出什么。
